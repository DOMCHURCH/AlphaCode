# --- core numerics ---
numpy>=1.26,<3
pandas>=2.2,<3

# --- data models & config ---
pydantic>=2.7,<3
pydantic-settings>=2.2,<3
python-dotenv>=1.0

# --- storage ---
SQLAlchemy>=2.0,<3
psycopg2-binary>=2.9
alembic>=1.13
# Redis is optional and not installed by default -- the cache and rate-limiter
# fall back to an in-process implementation, which is equivalent for a single
# service. To share rate-limit budgets across multiple processes, add `redis`
# here and set REDIS_URL.

# --- HTTP / resilience ---
httpx>=0.27
tenacity>=8.2

# --- API ---
fastapi>=0.110
uvicorn[standard]>=0.29
pandas-market-calendars>=4.4

# --- signed session cookies ---
# Used directly (URLSafeTimedSerializer), not via Starlette's
# SessionMiddleware: the cookie flags and lifetime belong to this app,
# and app-wide middleware is a large blast radius for one dashboard.
itsdangerous>=2.1,<3

# --- outgoing mail (key recovery) ---
# Imported lazily by src/mailer.py, so a container that predates this
# line has key recovery switched off rather than failing to boot.
agentmail>=0.5,<1

# --- observability ---
structlog>=24.1

# --- fallback data source (optional at runtime, wrapped in try/except) ---
yfinance>=0.2.40
